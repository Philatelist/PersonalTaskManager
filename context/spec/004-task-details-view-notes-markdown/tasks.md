# Tasks: Task Details View + Notes (Markdown)

- **Specification:** `context/spec/004-task-details-view-notes-markdown/`
- **Status:** Approved

---

- [ ] **Slice 1: Navigation skeleton — install deps, TaskDetailView shell, page/scroll preservation**
  *The smallest foundational change: install Markdown dependencies, replace the placeholder with a real TaskDetailView component that displays read-only task data (title, priority, timestamps, description, tags, due date, status badge) and wires up back-button navigation with page and scroll restoration. After this slice, clicking a card shows the detail view, and the back button restores the grid exactly where the user left off.*
  *Acceptance criteria covered: clicking card opens detail view; detail view displays all task fields; back button returns to grid on same page and scroll position; detail view is vertically scrollable; reuses loaded task data; "not found" redirects with toast; priority number displayed (read-only); timestamps displayed (read-only).*
  - [x] Install `react-markdown` and `rehype-sanitize` via `pnpm add react-markdown rehype-sanitize`. Verify packages appear in `package.json` and `pnpm install` succeeds. **[Agent: general-purpose]**
  - [x] Create `src/features/tasks/TaskDetailView.tsx` and `src/features/tasks/TaskDetailView.module.css`. The component receives `taskId: string`, `priorityIndex: number`, and `onBack: () => void` props. Use the existing `useTask(taskId)` hook to fetch task data. Render a vertically scrollable layout with: a back button ("← Back to Grid"), the task title as an `<h1>` (read-only for now), a metadata area showing status badge, priority number (`#{priorityIndex}`), due date (formatted or "No due date"), tags as plain text, and timestamps ("Created: ... · Updated: ..."). Render the description as plain text for now (Markdown rendering comes in Slice 3). Show a loading state while `useTask` is fetching. If the task is not found (error or null after loading), call `onBack()` and show a toast message "Task not found". **[Agent: general-purpose]**
  - [x] Create `src/features/tasks/Toast.tsx` and `src/features/tasks/Toast.module.css`. A simple notification component rendered via React portal to `document.body`. Props: `message: string`, `onDismiss: () => void`. Position fixed at bottom-center, auto-dismiss after 3 seconds via `useEffect` + `setTimeout`. Add fade-in/fade-out CSS transition. **[Agent: general-purpose]**
  - [x] Modify `App.tsx`: replace `TaskDetailPlaceholder` with `TaskDetailView`. Add `scrollPositionRef = useRef(0)`, `currentPageRef = useRef(1)`, and `priorityIndexRef = useRef(0)`. Modify `onSelectTask` to accept `(id: string, priorityIndex: number)` — capture `window.scrollY` in `scrollPositionRef`, capture `currentPageRef` and `priorityIndexRef`, then set `selectedTaskId`. On `onBack()`: clear `selectedTaskId`, restore scroll position using `useLayoutEffect` (fires after DOM mutation but before paint to prevent visual jump). Pass `currentPageRef.current` as an `initialPage` prop to `GridView`. **[Agent: general-purpose]**
  - [x] Modify `GridView.tsx`: accept an `initialPage?: number` prop. Use it as the initial value for `currentPage` state. Modify `onSelectTask` callback signature to `(id: string, priorityIndex: number)`. Compute `priorityIndex = startIndex + localIndex + 1` in `TaskGrid` and pass it up through the `onSelectTask` chain. Update `TaskGrid.tsx` accordingly — modify the `onSelectTask` prop to include the index, and compute it per card. **[Agent: general-purpose]**
  - [x] **Verify:** Create `TaskDetailView.test.tsx`: test renders all sections (title, metadata, description, back button); test loading state; test "not found" calls `onBack` and shows toast; test back button fires `onBack`. Create `Toast.test.tsx`: test renders message; test auto-dismiss after timeout; test `onDismiss` callback. Update `App.test.tsx` (if needed) and `GridView.test.tsx`: verify `initialPage` prop is respected; verify `onSelectTask` passes `priorityIndex`. Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — create 3+ tasks, click a card, verify detail view shows all fields correctly, click back, verify grid returns to same page and scroll position. **[Agent: general-purpose]**

- [ ] **Slice 2: Inline-editable title with autosave**
  *Add the EditableTitle component: click heading to edit, autosave on blur/Enter, cancel on Escape, reject empty strings. Wire debounced autosave in TaskDetailView with flush-on-unmount. After this slice, the user can rename tasks directly from the detail view.*
  *Acceptance criteria covered: title displayed as large heading; click transforms to editable input; auto-focus + select-all; autosave on blur/Enter; empty title rejected; Escape cancels; debounce cancel-safe on unmount; pending saves flushed before navigation.*
  - [x] Create `src/features/tasks/EditableTitle.tsx` and `src/features/tasks/EditableTitle.module.css`. Props: `value: string`, `onSave: (newTitle: string) => Promise<void>`. State: `isEditing`, `editValue`. Display: `<h1>` heading with `cursor: pointer`. Click → transforms to `<input type="text">` with auto-focus and `select()`. On blur or Enter: if `editValue.trim()` is empty, revert to `value` without saving; otherwise call `onSave(editValue.trim())` and exit edit mode. On Escape: revert `editValue` to `value`, exit edit mode, no save. Style the input to match the `<h1>` appearance (same font-size, weight, no visible border by default). **[Agent: general-purpose]**
  - [x] Modify `TaskDetailView.tsx`: replace the read-only `<h1>` with `<EditableTitle>`. Add `titleValueRef = useRef(task.title)` and `titleSaveTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)`. Implement `handleTitleSave(newTitle)`: no-op guard (if `newTitle === task.title`, return); clear any pending timer; call `taskUpdate(taskId, { title: newTitle })`; call `refresh()`. On unmount (`useEffect` cleanup): if `titleSaveTimerRef` has a pending timer, clear it and flush the save using `titleValueRef.current`. **[Agent: general-purpose]**
  - [x] **Verify:** Create `EditableTitle.test.tsx`: test click enters edit mode; test auto-focus and select-all; test Enter saves; test Escape cancels without saving; test empty string reverts; test blur saves. Extend `TaskDetailView.test.tsx`: test title editing calls `taskUpdate` with new value; test no-op guard (same value doesn't call `taskUpdate`); test autosave flushes on unmount. Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — open a task, click the title, edit it, press Enter, verify it saves (refresh app to confirm persistence). Test Escape reverts. Test empty string rejection. **[Agent: general-purpose]**

- [ ] **Slice 3: Markdown notes editor with toolbar and autosave**
  *Add MarkdownEditor and MarkdownToolbar: rendered Markdown view (default), click-to-edit textarea, formatting toolbar with 7 buttons, debounced autosave (~1s), flush on blur and unmount, HTML sanitization, safe links. After this slice, the user can write and format notes with Markdown.*
  *Acceptance criteria covered: description rendered as Markdown; empty placeholder shown; click enters edit mode; raw Markdown in textarea; toolbar with all 7 buttons; toolbar inserts correct syntax; click outside/Escape exits edit; autosave with debounce; cancel-safe on unmount; flush on navigation; all supported Markdown elements; HTML sanitized; links safe; 10K chars without lag.*
  - [x] Create `src/features/tasks/MarkdownToolbar.tsx` and `src/features/tasks/MarkdownToolbar.module.css`. Props: `textareaRef: RefObject<HTMLTextAreaElement>`, `onValueChange: (newValue: string) => void`. Render a horizontal toolbar with 7 buttons: Bold, Italic, Heading, Bullet List, Numbered List, Code, Link. Each button reads `selectionStart`/`selectionEnd` from the textarea ref, wraps the selection (or inserts placeholder text if no selection), updates the value via `onValueChange`, and restores the cursor position. Bold wraps in `**`, Italic in `*`, Heading inserts `# ` at line start (or cycles `#`→`##`→`###`→`#`), Bullet inserts `- ` at line start, Numbered inserts `1. ` at line start, Code wraps in backticks (single for inline, triple for multi-line selection), Link wraps as `[text](url)`. All buttons have `aria-label` attributes. Style as a compact row with icon-like labels and hover states. **[Agent: general-purpose]**
  - [ ] Create `src/features/tasks/MarkdownEditor.tsx` and `src/features/tasks/MarkdownEditor.module.css`. Props: `value: string`, `onChange: (newValue: string) => void`. State: `isEditing`. **Rendered view (default):** render `value` using `<ReactMarkdown rehypePlugins={[rehypeSanitize]} components={{ a: SafeLink }}>`. Define `SafeLink` component inline: renders `<a>` with `target="_blank"` and `rel="noopener noreferrer"`. If `value` is empty/null, show placeholder "Click to add notes..." with muted style. Click on rendered content or placeholder → enter edit mode. **Edit view:** render `<MarkdownToolbar>` above a `<textarea>` that auto-grows to content. The textarea is an uncontrolled input during editing (store value in a ref, only call `onChange` to report changes — this avoids re-renders on every keystroke for performance). Auto-focus textarea on mount. Exit edit: click outside the editor container (use `onBlur` with `relatedTarget` check to avoid exiting when clicking toolbar buttons), or press Escape. **[Agent: general-purpose]**
  - [ ] Modify `TaskDetailView.tsx`: replace the plain-text description with `<MarkdownEditor>`. Add `descriptionValueRef = useRef(task.description ?? "")` and `descriptionSaveTimerRef`. On `handleDescriptionChange(newValue)`: update `descriptionValueRef.current = newValue`; clear existing timer; set new timer (~1000ms) → if `newValue !== task.description`, call `taskUpdate(taskId, { description: newValue })` then `refresh()`. On unmount: flush pending description save if timer exists, reading from `descriptionValueRef`. On blur (editor exits edit mode): flush immediately (clear timer, save if changed). **[Agent: general-purpose]**
  - [ ] **Verify:** Create `MarkdownToolbar.test.tsx`: test each of the 7 buttons inserts correct syntax with selection; test each button inserts placeholder when no selection; test cursor position is restored after insertion. Create `MarkdownEditor.test.tsx`: test renders Markdown (bold, heading, list, code); test placeholder shown when empty; test click enters edit mode; test Escape exits edit mode; test `onChange` fires on keystroke; test raw HTML is sanitized (e.g., `<script>` tag not rendered); test links have `target="_blank"` and `rel="noopener noreferrer"`. Extend `TaskDetailView.test.tsx`: test description autosave with debounce (use `vi.useFakeTimers`); test flush on unmount; test no-op guard for unchanged value. Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — open a task, click the description area, type Markdown (`**bold** and *italic*`), click outside, verify rendered formatting. Test toolbar buttons. Test autosave (edit, wait, refresh app). **[Agent: general-purpose]**

- [ ] **Slice 4: Tags editor + due date picker**
  *Add TagEditor (chips with add/remove) and DueDatePicker (click-to-edit with native picker and clear). Both persist immediately without debounce. After this slice, the metadata section is fully interactive.*
  *Acceptance criteria covered: tags as chips in alphabetical order; add tag on Enter; duplicates ignored; remove tag via X; tag changes persist immediately; due date formatted; overdue highlighted; placeholder for no date; click opens picker; date persists immediately; clear button removes date.*
  - [ ] Create `src/features/tasks/TagEditor.tsx` and `src/features/tasks/TagEditor.module.css`. Props: `tags: string[]`, `onAdd: (tag: string) => void`, `onRemove: (tag: string) => void`. Render tags as chip/badge elements in alphabetical order, each with a remove "×" button (with `aria-label="Remove tag {tag}"`). Below the chips, render a text input with placeholder "Add tag...". On Enter: if input is non-empty and trimmed, call `onAdd(value.trim())`, clear input. Input clears regardless of duplicate (backend deduplicates silently). Style chips with background color, rounded corners, compact padding. **[Agent: general-purpose]**
  - [ ] Create `src/features/tasks/DueDatePicker.tsx` and `src/features/tasks/DueDatePicker.module.css`. Props: `dueDate: string | null`, `onChange: (date: string | null) => void`. **Display mode:** if `dueDate` is set, show formatted date (e.g., "Feb 15, 2025") using `new Date(dueDate + "T00:00:00")` to avoid timezone shift. If overdue (date < today), apply `.overdue` class (red text). If no due date, show "No due date" in muted text. **Edit mode:** clicking the displayed date or placeholder reveals a native `<input type="date">`. On date change: call `onChange(selectedDate)`. Clear button (×): calls `onChange(null)`. Include `aria-label` for the date input and clear button. **[Agent: general-purpose]**
  - [ ] Modify `TaskDetailView.tsx`: integrate `<TagEditor>` and `<DueDatePicker>` into the metadata section. Implement `handleAddTag(tag)`: call `taskUpdate(taskId, { tags: [...task.tags, tag] })` then `refresh()`. Implement `handleRemoveTag(tag)`: call `taskUpdate(taskId, { tags: task.tags.filter(t => t !== tag) })` then `refresh()`. Implement `handleDueDateChange(date)`: call `taskUpdate(taskId, { dueDate: date })` then `refresh()`. All three are immediate (no debounce). **[Agent: general-purpose]**
  - [ ] **Verify:** Create `TagEditor.test.tsx`: test renders chips in alphabetical order; test add tag on Enter clears input; test duplicate silently ignored (callback still fires — backend deduplicates); test remove button fires `onRemove`; test empty input does nothing on Enter. Create `DueDatePicker.test.tsx`: test shows formatted date; test shows "No due date" placeholder; test click opens date input; test overdue date has `.overdue` class; test clear button calls `onChange(null)`; test selecting date calls `onChange`. Extend `TaskDetailView.test.tsx`: test tag add/remove calls `taskUpdate` with correct tags array; test due date change calls `taskUpdate`. Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — open a task, add tags, remove tags, set due date, clear due date, verify all changes persist (refresh app). **[Agent: general-purpose]**

- [ ] **Slice 5: Status management — Mark as Done, Reactivate, Delete**
  *Add StatusActions component: status badge, "Mark as Done" for active tasks (navigates back to grid preserving page), "Reactivate" for done tasks, "Delete" with confirmation prompt. After this slice, the full task lifecycle is manageable from the detail view.*
  *Acceptance criteria covered: current status visually displayed; Mark as Done changes status and returns to grid; grid preserves page after done; Reactivate changes status to active; Delete with confirmation soft-deletes and returns to grid; all status changes persist immediately.*
  - [ ] Create `src/features/tasks/StatusActions.tsx` and `src/features/tasks/StatusActions.module.css`. Props: `status: TaskStatus`, `onMarkDone: () => void`, `onReactivate: () => void`, `onDelete: () => void`. Render a status badge (Active = green background, Done = gray background). For active tasks: render "Mark as Done" button. For done tasks: render "Reactivate" button. Always render a "Delete" button in a danger-styled area. On Delete click: call `window.confirm("Are you sure you want to delete this task?")` — if confirmed, call `onDelete()`. All buttons have appropriate `aria-label` attributes. **[Agent: general-purpose]**
  - [ ] Modify `TaskDetailView.tsx`: integrate `<StatusActions>` into the layout. Implement `handleMarkDone()`: call `taskUpdate(taskId, { status: "done" })`, then call `onBack()` (the grid restores its previously saved `currentPageRef`). Implement `handleReactivate()`: call `taskUpdate(taskId, { status: "active" })`, then `refresh()` to show updated status in the detail view (user remains on detail view and can navigate back). Implement `handleDelete()`: call `taskDelete(taskId)`, then call `onBack()`. Import `taskDelete` from `task-service`. **[Agent: general-purpose]**
  - [ ] **Verify:** Create `StatusActions.test.tsx`: test active task shows "Mark as Done" button and no "Reactivate"; test done task shows "Reactivate" and no "Mark as Done"; test Delete shows confirmation; test confirmed Delete fires `onDelete`; test cancelled Delete does not fire; test status badge displays correct status. Extend `TaskDetailView.test.tsx`: test Mark as Done calls `taskUpdate` with `status: "done"` then `onBack`; test Delete calls `taskDelete` then `onBack`; test Reactivate calls `taskUpdate` with `status: "active"`. Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — open a task, click "Mark as Done", verify returned to grid and task is gone from active view. Create a done-status task (if possible), verify "Reactivate" appears. Test Delete with confirmation. **[Agent: general-purpose]**

- [ ] **Slice 6: Subtask list — display, add, toggle, delete, drag-to-reorder**
  *Add SubtaskSection and SubtaskItem for checklist subtasks. Display list with checkboxes, add new items via input, toggle completion, delete, drag-to-reorder with same-position guard. After this slice, checklist subtasks are fully manageable from the detail view.*
  *Acceptance criteria covered: subtasks displayed in order; checklist shows checkbox + label; add input creates subtask on Enter; new subtasks at bottom; toggle persists immediately; delete removes immediately; drag-to-reorder persists; same-position guard; empty state shows only add input; subtask drag smooth.*
  - [ ] Create `src/features/tasks/SubtaskItem.tsx` and `src/features/tasks/SubtaskItem.module.css`. Props: `subtask: Subtask`, `onToggle: (id: string, isDone: boolean) => void`, `onDelete: (id: string) => void`. Integrate `useSortable(subtask.id)` for drag handle. Render a horizontal row with: drag handle (⠿ grip icon with `data-subtask-drag-handle`), checkbox (`checked={subtask.isDone}`), label text, and delete button (× icon). For checklist type: checkbox click calls `onToggle(subtask.id, !subtask.isDone)`. For taskref type (handled in Slice 7): checkbox is disabled. Style with hover state for delete button visibility. Apply `transform`/`transition` from `useSortable`. **[Agent: general-purpose]**
  - [ ] Create `src/features/tasks/SubtaskSection.tsx` and `src/features/tasks/SubtaskSection.module.css`. Props: `subtasks: Subtask[]`, `taskId: string`, `onUpdated: () => void`. Wrap subtask list in `DndContext` + `SortableContext` with `verticalListSortingStrategy`. Configure sensors: `PointerSensor` (distance: 5), `KeyboardSensor`. On drag end: compute new `orderedIds` array; **same-position guard** — compare with current order, skip if identical; otherwise call `subtaskReorder(taskId, orderedIds)` then `onUpdated()`. Render `SubtaskItem` for each subtask. Below the list, render an "Add subtask" text input: on Enter with non-empty value → call `subtaskCreate(taskId, "checklist", label)` → clear input → `onUpdated()`. Show `DragOverlay` with a clone of the dragged subtask item. Empty state: if no subtasks, show only the add input. **[Agent: general-purpose]**
  - [ ] Modify `TaskDetailView.tsx`: integrate `<SubtaskSection>` below the Markdown editor. Pass `task.subtasks`, `taskId`, and `onUpdated={refresh}`. Implement toggle handler: call `subtaskUpdate(subtaskId, { isDone })` then `refresh()`. Implement delete handler: call `subtaskDelete(subtaskId)` then `refresh()`. Pass these through `SubtaskSection` → `SubtaskItem`. **[Agent: general-purpose]**
  - [ ] **Verify:** Create `SubtaskItem.test.tsx`: test renders checkbox and label; test checkbox click fires `onToggle`; test delete button fires `onDelete`; test drag handle rendered with correct attribute. Create `SubtaskSection.test.tsx`: test renders subtask list in order; test add input creates subtask on Enter; test empty input does nothing; test empty state shows only input; test reorder calls `subtaskReorder`; test same-position drop skips backend call. Extend `TaskDetailView.test.tsx`: test subtask toggle calls `subtaskUpdate` then `refresh`; test subtask delete calls `subtaskDelete` then `refresh`. Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — open a task, add 3 subtask items, check one off, delete one, drag to reorder, verify persistence (refresh app). Return to grid and verify progress ring updates. **[Agent: general-purpose]**

- [ ] **Slice 7: TaskRef subtasks — search modal, circular reference protection (frontend + backend)**
  *Add TaskRefSearchModal for linking tasks as subtasks. Add frontend filtering (excludes self + circular refs) and backend guard in create_subtask_impl. Handle deleted-ref display. After this slice, taskref subtasks are fully supported with integrity checks.*
  *Acceptance criteria covered: Link task mechanism with search; task cannot reference itself; direct circular refs rejected with notification; taskref shows referenced title + status; taskref checkbox reflects ref status (non-toggleable); deleted ref shows "Deleted task"; empty search state.*
  - [ ] Create `src/features/tasks/TaskRefSearchModal.tsx` and `src/features/tasks/TaskRefSearchModal.module.css`. Props: `taskId: string`, `onSelect: (refTaskId: string) => void`, `onClose: () => void`. Render via React portal. On mount, fetch all active tasks via `taskList({ statusFilter: "active" })`. Filter out: the current task (self-reference); tasks whose subtasks include a taskref pointing back to the current task (circular prevention — check each candidate's `subtasks` array for `type === "taskref" && refTaskId === taskId`). Render a search input that filters remaining candidates by case-insensitive title substring. Show a scrollable list of matching tasks (title + status badge). If no matches: show "No matching tasks" empty state. On task click: call `onSelect(refTaskId)`, call `onClose()`. Dismiss on Escape or click outside. **[Agent: general-purpose]**
  - [ ] Modify `SubtaskSection.tsx`: add a "Link task" button below the "Add subtask" input. On click, open `TaskRefSearchModal`. On select from modal: call `subtaskCreate(taskId, "taskref", null, refTaskId)`, then `onUpdated()`. If the backend returns an error containing "CircularTaskRefNotAllowed" or "cannot reference itself", show a toast notification with a user-friendly message and do not add the subtask. **[Agent: general-purpose]**
  - [ ] Modify `SubtaskItem.tsx`: handle taskref subtask display. If `subtask.type === "taskref"`: show the referenced task's title (`subtask.refTaskTitle`) instead of `subtask.label`. Show a small status badge for the referenced task's status (`subtask.refTaskStatus`). Checkbox: if `refTaskStatus === "done"`, show checked (disabled); otherwise show unchecked (disabled). If `refTaskStatus === "deleted"`, display "Deleted task" in grayed-out text, checkbox disabled. **[Agent: general-purpose]**
  - [ ] Modify `src-tauri/src/commands.rs`: in `create_subtask_impl`, add validation when `subtask_type == "taskref"` and `ref_task_id` is `Some`: (1) reject if `ref_task_id == task_id` with `Err("A task cannot reference itself")`; (2) query `SELECT COUNT(*) FROM subtasks WHERE task_id = ?1 AND type = 'taskref' AND ref_task_id = ?2` where `?1 = ref_task_id` and `?2 = task_id` — if count > 0, return `Err("CircularTaskRefNotAllowed: the target task already references this task")`. **[Agent: general-purpose]**
  - [ ] **Verify:** Create `TaskRefSearchModal.test.tsx`: test filters tasks by case-insensitive title; test excludes self; test excludes tasks with circular ref; test selecting fires `onSelect`; test "No matching tasks" empty state; test Escape closes. Extend `SubtaskItem.test.tsx`: test taskref shows referenced title; test taskref checkbox is disabled; test taskref with done ref shows checked+disabled; test deleted ref shows "Deleted task". Add Rust tests in `commands.rs`: `test_create_subtask_self_reference_rejected` — verify error; `test_create_subtask_circular_reference_rejected` — Task A refs B, then B trying to ref A fails; `test_create_subtask_non_circular_reference_allowed` — Task A refs B, Task C refs A is allowed. Run `pnpm test` and `cargo test` — all tests pass. Then `pnpm tauri dev` — open a task, click "Link task", search for another task, select it, verify taskref subtask appears with correct status badge. **[Agent: general-purpose]**

- [ ] **Slice 8: Optimistic UI hardening, error toasts, accessibility, edge cases**
  *Final hardening slice: wire optimistic UI with error recovery and toast notifications for all mutations, add no-op guards, audit keyboard accessibility (focusable controls, Escape consistency, aria-labels), verify updatedAt semantics, and test all edge cases. After this slice, all 68 acceptance criteria are met.*
  *Acceptance criteria covered: all save operations use optimistic UI; backend failure reverts UI + shows toast; non-blocking error notification; all controls keyboard-focusable; Escape exits edit modes consistently; form inputs have labels/aria-labels; updatedAt reflects all changes.*
  - [ ] Add optimistic UI to all mutation handlers in `TaskDetailView.tsx`: for `handleAddTag` / `handleRemoveTag` — optimistically update `task.tags` in local state before the backend call, revert via `refresh()` on error, show error toast. For `handleDueDateChange` — optimistically update `task.dueDate`. For subtask toggle/delete — optimistically update `task.subtasks` array. Ensure all mutation `catch` blocks show a `Toast` with the error message and call `refresh()` to revert. Add no-op guards: title save skips if value unchanged; description save skips if value unchanged (these should already be in place from Slices 2-3, verify and add if missing). **[Agent: general-purpose]**
  - [ ] Accessibility audit and fixes across all detail view components: ensure all interactive elements (toolbar buttons, tag remove ×, subtask delete ×, subtask drag handles, back button, status buttons, date picker, clear button) are `<button>` elements (or have `role="button"`) and are keyboard-focusable. Add `aria-label` to any icon-only buttons that lack visible text. Verify Escape consistently exits edit modes (title editing, markdown editing) — test and fix any gaps. Ensure the "Add subtask" input has `aria-label="Add subtask"`. Ensure tag input has `aria-label="Add tag"`. **[Agent: general-purpose]**
  - [ ] **Verify:** Extend `TaskDetailView.test.tsx`: test optimistic UI for tag add (local state updates before backend); test error toast appears on backend failure; test UI reverts on error. Test no-op guard for title (same value → no `taskUpdate` call). Test no-op guard for description. Extend `SubtaskSection.test.tsx`: test optimistic reorder (local array updates before backend call). Audit all test files for `aria-label` assertions on icon-only buttons. Run `pnpm test` — all tests pass, including all new tests from Slices 1-8. Run `cargo test` — all backend tests pass. Then `pnpm tauri dev` — comprehensive manual walkthrough: create a task, edit title, add Markdown notes, add tags, set due date, add checklist subtasks, link a taskref subtask, reorder subtasks, mark done, reactivate, delete. Verify all persistence, error recovery (simulate by temporarily breaking a service call), and keyboard navigation. **[Agent: general-purpose]**

---

## Subagent Coverage

All tasks are assigned to `general-purpose` since no specialist subagents exist. Verification relies on `pnpm test`, `cargo test`, and the Tauri dev server:

| Task/Slice | Issue | Recommendation |
|---|---|---|
| All Frontend sub-tasks (Slices 1–8) | Assigned to `general-purpose` — no React/TypeScript specialist | Consider adding a React/TypeScript agent in `.claude/agents/` |
| All Verification steps | No browser MCP available | UI verification done via Tauri dev server, test suites (`pnpm test`) |
| Slice 7: Backend sub-task | Assigned to `general-purpose` — no Rust specialist | Consider adding a Rust agent in `.claude/agents/` |
