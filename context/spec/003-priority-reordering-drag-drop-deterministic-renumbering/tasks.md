# Tasks: Priority Reordering (Drag & Drop) + Deterministic Renumbering

- **Specification:** `context/spec/003-priority-reordering-drag-drop-deterministic-renumbering/`
- **Status:** Approved

---

- [x] **Slice 1: Global priority numbering + install @dnd-kit**
  *The smallest foundational change: fix priority numbers from page-relative to global, and install the DnD dependency. After this slice, page 2 correctly shows #11, #12, etc. instead of #1, #2. No drag behavior yet — just the numbering fix and the dependency ready for Slice 2.*
  *Acceptance criteria covered: priority numbers reflect global position across all pages; numbers are sequential with no gaps or duplicates.*
  - [x] Install `@dnd-kit/core`, `@dnd-kit/sortable`, and `@dnd-kit/utilities` via `pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`. Verify the packages appear in `package.json` dependencies and `pnpm install` succeeds. **[Agent: general-purpose]**
  - [x] Modify `TaskGrid.tsx`: replace the `priorityIndex={index + 1}` prop with `globalIndex={startIndex + index + 1}`. Add a new `startIndex: number` prop (default 0). Modify `TaskCard.tsx`: rename `priorityIndex` prop to `globalIndex` and update the display from `#{priorityIndex}` to `#{globalIndex}`. Modify `GridView.tsx`: compute `startIndex = (currentPage - 1) * cardsPerPage` and pass it to `TaskGrid`. **[Agent: general-purpose]**
  - [x] **Verify:** Update existing tests: `TaskCard.test.tsx` — rename `priorityIndex` to `globalIndex` in all test renders. `TaskGrid.test.tsx` — add `startIndex` prop. `GridView.test.tsx` — add a test: with `cardsPerPage=2` and 5 tasks, navigate to page 2 and verify the first card shows `#3` (not `#1`). Run `pnpm test` — all tests pass. Then run `pnpm tauri dev` — create 5+ tasks, resize window to trigger pagination, navigate to page 2, confirm priority numbers start at `cardsPerPage + 1` instead of `#1`. **[Agent: general-purpose]**

- [ ] **Slice 2: Drag handle + sortable cards (basic drag-and-drop reorder)**
  *The core drag experience: wrap TaskGrid in DndContext/SortableContext, add drag handle to TaskCard, implement ghost card + drop indicator, wire onDragEnd to taskReorder() with optimistic arrayMove(). After this slice, the user can drag cards within a page and see them reorder with correct global numbers persisted to the backend.*
  *Acceptance criteria covered: drag handle displayed; drag only from handle; ghost card follows cursor; drop indicator shows insertion point; drop reorders globally and persists; all numbers update immediately; same-position guard (no unnecessary writes); Escape cancels drag; drag limited to current page.*
  - [x] Modify `TaskCard.tsx`: integrate `useSortable(task.id)` from `@dnd-kit/sortable`. Apply `transform` and `transition` from `useSortable` to the card root element. Add a drag handle element (6-dot grip icon) positioned absolute top-right, applying `listeners` and `attributes` from `useSortable` to the handle only (NOT the entire card). Add `data-task-id={task.id}` to the card root and `data-drag-handle` to the handle element. Add `.dragHandle` and `.dragging` (opacity: 0.5 when `isDragging`) CSS classes to `TaskCard.module.css`. The handle must NOT have an onClick for card navigation — card `onClick` remains on the article element which is unaffected by the handle's drag listeners. **[Agent: general-purpose]**
  - [x] Modify `TaskGrid.tsx`: wrap the grid in `DndContext` and `SortableContext` from `@dnd-kit`. Configure sensors: `PointerSensor` with `activationConstraint: { distance: 5 }` (prevents accidental drags). Set `collisionDetection={closestCenter}` and `strategy={rectSortingStrategy}`. Add a `DragOverlay` that renders a clone of the dragged `TaskCard` with reduced opacity. Add `onReorder: (taskId: string, afterId: string | null) => void` prop. In `onDragEnd`, map the new drop position to `afterId` per the reorder logic: position 0 on page 1 → `afterId = null`; position 0 on page N → `afterId = tasks[startIndex - 1].id`; position i > 0 → `afterId = pageTasks[i - 1].id`. Add same-position guard: if old index equals new index, return early without calling `onReorder`. Pass the full `tasks` array (from `useTasks`) as `allTasks` prop so the grid can resolve cross-page `afterId` lookups. **[Agent: general-purpose]**
  - [x] Modify `GridView.tsx`: implement `handleReorder(taskId, afterId)` — (1) optimistically reorder the local `tasks` array using `arrayMove()` so the UI updates instantly, (2) call `taskReorder(taskId, afterId)` to persist, (3) await completion then call `refresh()` to confirm backend state, (4) if the backend call throws, call `refresh()` to revert the optimistic update. Pass `handleReorder` as the `onReorder` prop to `TaskGrid`, and pass the full `tasks` array as `allTasks`. **[Agent: general-purpose]**
  - [x] Modify `useTasks` hook (`use-tasks.ts`): expose a `setTasks` function (or an `optimisticReorder(fromIndex, toIndex)` helper) so GridView can update the local task array optimistically before the backend confirms. This avoids duplicating state — GridView uses the same `tasks` array from `useTasks`. **[Agent: general-purpose]**
  - [x] **Verify:** Update `TaskCard.test.tsx`: add tests — drag handle element is rendered with correct attributes (`data-drag-handle`); clicking the drag handle does NOT fire `onSelect`; card click still fires `onSelect`. Update `TaskGrid.test.tsx` (or create new): mock `DndContext` if needed; verify `onReorder` is called with correct `afterId` for different drop positions; verify same-position drop does NOT call `onReorder`. Update `GridView.test.tsx`: mock `taskReorder` from task-service; verify `handleReorder` calls `taskReorder` then `refresh`; verify optimistic update reorders local array before backend confirms. Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — create 5 tasks, drag a card from position #3 to position #1 via the drag handle, verify all cards renumber correctly, refresh the app and confirm the order persists. Verify clicking the card body (not handle) still opens the detail placeholder. Verify Escape during drag cancels cleanly. **[Agent: general-purpose]**

- [ ] **Slice 3: Keyboard reordering (Alt+Arrow Up/Down)**
  *Add the keyboard sensor with custom Alt+Arrow key bindings. After this slice, the user can focus a card's drag handle and press Alt+ArrowUp/Down to move it up/down in global priority. Focus remains on the moved card for repeated moves.*
  *Acceptance criteria covered: card focusable via keyboard; Alt+ArrowUp moves earlier in global list; Alt+ArrowDown moves later; no-op at boundaries; each move persists and updates numbers; focus preserved; repeated moves work.*
  - [x] Add `KeyboardSensor` to the sensors array in `TaskGrid.tsx`. Create a custom `coordinateGetter` function (e.g., in `src/features/tasks/keyboard-coordinates.ts`) that only responds to `Alt+ArrowUp` and `Alt+ArrowDown` — ignores all other keys. Alt+ArrowUp decrements the sort index by one; Alt+ArrowDown increments by one. Call `event.preventDefault()` on matched events to prevent browser default behavior (Windows Alt+Arrow = browser back/forward). Plain arrow keys are ignored by this sensor. **[Agent: general-purpose]**
  - [x] Implement focus preservation in `GridView.tsx`: add a `movedTaskIdRef` that stores the task ID being reordered. After `handleReorder` completes and `tasks` state updates (via a `useEffect` watching `tasks` and `movedTaskIdRef`), locate the handle element via `document.querySelector('[data-task-id="${id}"] [data-drag-handle]')` and call `.focus()`. Clear the ref after focusing. This ensures the user can press Alt+Arrow repeatedly without re-selecting the card. **[Agent: general-purpose]**
  - [x] **Verify:** Write tests: in `TaskGrid.test.tsx` or a new `keyboard-reorder.test.tsx` — verify the custom `coordinateGetter` responds to Alt+ArrowUp/Down and ignores plain arrows; verify `event.preventDefault()` is called. In `GridView.test.tsx` — add test: after a keyboard reorder, the moved card's handle retains focus (mock `document.querySelector` and verify `.focus()` is called with the correct selector). Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — Tab to focus a card's drag handle, press Alt+ArrowDown twice, verify the card moves down two positions with correct numbering, verify focus stays on the card throughout. Try Alt+ArrowUp on the #1 card — verify nothing happens. **[Agent: general-purpose]**

- [ ] **Slice 4: Context menu — "Move to Top" and "Move to Bottom"**
  *Add the right-click context menu with cross-page move shortcuts. After this slice, the user can right-click any card and jump it to priority #1 or to last, with automatic page navigation.*
  *Acceptance criteria covered: right-click opens context menu; Move to Top moves to #1 and navigates to page 1; Move to Bottom moves to last and navigates to last page; numbers update across pages; moves persist immediately; no-op guard for already-last task.*
  - [x] Create `src/features/tasks/TaskCardContextMenu.tsx` and `src/features/tasks/TaskCardContextMenu.module.css`. The component receives `position: {x, y}`, `taskId`, `onMoveToTop`, `onMoveToBottom`, and `onClose` props. Render via React portal (`document.body`). Position absolute at click coordinates, clamped to viewport bounds. Two menu items: "Move to Top" and "Move to Bottom". Dismissal: click outside (use `useEffect` with document click listener), Escape key (keydown listener), or selecting an item. On item select: close the menu first (call `onClose()`), then invoke the action callback. Call `event.preventDefault()` and `event.stopPropagation()` on the `onContextMenu` trigger to suppress native menu and card click. **[Agent: general-purpose]**
  - [x] Wire context menu into `GridView.tsx` and `TaskCard.tsx`: add `onContextMenu` handler to `TaskCard` that calls `event.preventDefault()`, `event.stopPropagation()`, and fires a new `onContextMenu(taskId, {x, y})` callback prop. In `GridView`, manage context menu state: `{taskId, position} | null`. Implement `handleMoveToTop(taskId)`: call `taskReorder(taskId, null)`, await, refresh, then `setCurrentPage(1)`. Implement `handleMoveToBottom(taskId)`: check if task is already last (compare `taskId === tasks[tasks.length - 1].id`) — if so, return early; otherwise compute `afterId = tasks.filter(t => t.id !== taskId).at(-1)!.id`, call `taskReorder(taskId, afterId)`, await, refresh, then navigate to last page (`setCurrentPage(totalPages)`). Render `TaskCardContextMenu` conditionally when context menu state is set. **[Agent: general-purpose]**
  - [x] **Verify:** Create `TaskCardContextMenu.test.tsx`: right-click renders menu with two items; "Move to Top" calls `onMoveToTop`; "Move to Bottom" calls `onMoveToBottom`; click outside closes menu; Escape closes menu; native context menu is suppressed (`preventDefault` called). Extend `GridView.test.tsx`: mock `taskReorder`; right-click a card → context menu appears; select "Move to Top" → `taskReorder(id, null)` called, page navigates to 1; "Move to Bottom" on non-last task → correct `afterId` computed; "Move to Bottom" on already-last task → no `taskReorder` call (no-op). Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — create 20+ tasks with pagination, right-click a card on page 2, select "Move to Top", verify it's #1 on page 1. Right-click #1, select "Move to Bottom", verify it's last on last page. **[Agent: general-purpose]**

- [ ] **Slice 5: Concurrent change safety + edge case hardening**
  *Add tasksVersion tracking to safely cancel drags when the underlying data changes mid-operation. Verify all edge cases end-to-end: optimistic revert on failure, renumbering invisible to user, drop-at-page-boundary correctness.*
  *Acceptance criteria covered: concurrent change cancels drag with no reorder; optimistic revert on backend failure; renumbering invisible; drop at page boundary inserts correctly in global order.*
  - [x] Implement concurrent change safety in `GridView.tsx`: add a `tasksVersionRef = useRef(0)`. Increment it in `refresh()` (or in the `useTasks` hook whenever `tasks` state changes). On `onDragStart` (new callback from `TaskGrid`), capture `dragStartVersion = tasksVersionRef.current` in a ref. On `onDragEnd`, before calling `handleReorder`, check `tasksVersionRef.current !== dragStartVersion` — if stale, discard the event (return early, no `taskReorder` call). Wire `onDragStart` through `TaskGrid` from the `DndContext` `onDragStart` handler. Also handle optimistic revert: if `taskReorder()` throws in `handleReorder`, call `refresh()` to restore the true backend state, rolling back the optimistic `arrayMove()`. **[Agent: general-purpose]**
  - [x] **Verify:** Extend `GridView.test.tsx`: test concurrent safety — simulate a drag start, then trigger a tasks change (mock `useTasks` returning new tasks), then simulate drag end → verify `taskReorder` is NOT called. Test optimistic revert — mock `taskReorder` to reject, verify `refresh()` is called to revert. Test page-boundary drop: with `cardsPerPage=3` on page 2 (tasks 4-6), drop at position 0 → verify `afterId` is task #3's ID (last task of page 1). Drop at last position on page 2 (not last page) → verify `afterId` is task #6's ID (inserts before task #7). Run `pnpm test` — all tests pass. Then `pnpm tauri dev` — with 20+ tasks: mark a task as done via checkbox while another card is mid-drag → verify drag cancels cleanly; perform 30+ rapid reorders and verify no ordering glitches or stale numbers (renumbering works silently). **[Agent: general-purpose]**

---

## Subagent Coverage

All tasks are assigned to `general-purpose` since no specialist subagents exist. Verification relies on `pnpm test` and the Tauri dev server:

| Task/Slice | Issue | Recommendation |
|---|---|---|
| All Frontend sub-tasks (Slices 1–5) | Assigned to `general-purpose` — no React/TypeScript specialist | Consider adding a React/TypeScript agent in `.claude/agents/` |
| All Verification steps | No browser MCP available | UI verification done via Tauri dev server, test suites (`pnpm test`) |
